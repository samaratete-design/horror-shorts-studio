# Local packages (editable installs) - order matters (dependency order)
-e packages/shared_types
-e packages/provider_abstractions
-e packages/originality_engine
-e packages/story_engine

# API
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
sqlalchemy[asyncio]>=2.0.30
asyncpg>=0.29.0
alembic>=1.13.0
pgvector>=0.3.0
pydantic>=2.7.0
pydantic-settings>=2.3.0

# Providers
anthropic>=0.34.0

# Background jobs (wired in Phase 5/6)
redis>=5.0.0

# Dev / test
pytest>=8.2.0
pytest-asyncio>=0.23.0
httpx>=0.27.0
